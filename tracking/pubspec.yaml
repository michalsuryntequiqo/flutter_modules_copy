name: tracking
description: Universal tracking layer for Flaconi Flutter projects.
version: 1.1.0
author:
homepage:

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^3.0.0
